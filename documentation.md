# 📚 Notebook System V4 Documentation

เอกสารรายละเอียดเชิงเทคนิคและการทำงานของระบบยืม-คืนอุปกรณ์ฉบับสมบูรณ์

---

## 1. ภาพรวม (Overview)
**Notebook System V4** เป็นระบบเว็บแอปพลิเคชันสำหรับจัดการการยืม-คืนอุปกรณ์โน้ตบุ๊คและอุปกรณ์ส่วนกลาง พัฒนาด้วยเทคโนโลยีสมัยใหม่ เน้นความรวดเร็ว ความปลอดภัย และการตอบสนองแบบ Real-time โดยมีฟีเจอร์หลักคือการจอง (Reservation), การยืม (Loan), การยืมพิเศษ (Special Loan) และการแจ้งเตือนผ่าน Discord

### Tech Stack หลัก:
- **Framework:** Next.js 15+ (App Router)
- **Language:** TypeScript
- **Database & Auth:** Supabase (PostgreSQL)
- **Styling:** TailwindCSS
- **State & Data Fetching:** React Query & Server Actions
- **Notification:** Discord Webhook

---

## 2. โครงสร้างโฟลเดอร์ (Folder Structure)
```
notebook-system-v4/
├── app/                    # Next.js App Router (Routes & Pages)
│   ├── admin/              # จัดการระบบ สำหรับผู้ดูแล
│   ├── staff/              # ระบบปฏิบัติงาน สำหรับเจ้าหน้าที่
│   ├── equipment/          # หน้าแสดงรายการอุปกรณ์
│   ├── auth/               # ระบบ Authentication callback
│   └── (profile, loans...) # หน้าเพจสำหรับผู้ใช้งานทั่วไป
├── components/             # Reusable React UI Components
├── hooks/                  # Custom React Hooks
├── lib/                    # Logic ชั้นกลาง (Server Actions, API, Tools)
│   ├── supabase/           # การตั้งค่าเชื่อมต่อ Supabase
│   ├── data/               # ข้อมูล Static หรือ Helper
│   └── ...                 # ระบบแจ้งเตือน, รูปแบบวันที่ Thai
├── supabase/               # Backend Logic
│   └── migrations/         # ไฟล์ SQL สำหรับสร้างตาราง และ RLS
├── public/                 # ไฟล์ Static (รูปภาพ, ไอคอน)
└── docs/                   # เอกสารคู่มือการใช้งานอื่น ๆ
```

---

## 3. การติดตั้ง (Installation)
1. **Clone Repository และติดตั้ง Dependencies:**
   ```bash
   npm install
   ```
2. **ตั้งค่า Environment Variables:**
   สร้างไฟล์ `.env.local` ใน Root directory และใส่ค่าจาก Supabase Project:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=your-project-url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
   ```
3. **รันระบบ (Development Mode):**
   ```bash
   npm run dev
   ```
4. **Build สำหรับ Production:**
   ```bash
   npm run build
   npm start
   ```

---

## 4. อธิบายระบบอัตโนมัติ (Automation)
ระบบมีการทำงานอัตโนมัติในส่วนหลัก ๆ ดังนี้:
- **Discord Notifications:** เมื่อมีการจองใหม่, คืนอุปกรณ์, หรือความผิดปกติ ระบบจะส่งความความไปยัง Webhook ที่ตั้งค่าไว้โดยอัตโนมัติ (`lib/notifications.ts`)
- **Auto-Approval:** สำหรับผู้ใช้ที่มีบทบาท `staff` หรือ `admin` เมื่อทำการจองระบบจะอนุมัติ (Status: `approved`) ให้ทันทีโดยไม่ต้องผ่านขั้นตอนตรวจสอบ
- **Activity Logging:** บันทึกทุกกิจกรรมของเจ้าหน้าที่ (Staff Activity) ลงในตาราง `staff_activity_log` เพื่อใช้ในการตรวจสอบย้อนหลัง (Audit Path)
- **Inventory Alerts:** ระบบตรวจสอบจำนวนอุปกรณ์คงเหลือ หากถึงระดับวิกฤตจะมีการส่งแจ้งเตือน

---

## 5. Config & Environment
ระบบใช้ไฟล์ `.env.local` ในการจัดการ Config หลัก และมีตาราง `system_config` ในฐานข้อมูลเพื่อเก็บค่าคอนฟิกที่สามารถแก้ไขได้ผ่านหน้าเว็บ (Dynamic Config) เช่น:
- URL ของ Discord Webhooks แยกตามประเภท (General, Auth, Reservation)
- จำนวนอุปกรณ์สูงสุดที่แต่ละบทบาทสามารถยืมได้
- เวลาเปิด-ปิดระบบอัตโนมัติ

---

## 6. HTTP Layer
ระบบใช้ `@supabase/ssr` ในการจัดการ HTTP Request และ Session:
- **Client Client:** ใช้ใน Client Components สำหรับการ Fetch ข้อมูลที่ต้องการความเร็วหรือ Real-time (`lib/supabase/client.ts`)
- **Server Client:** ใช้ใน Server Components และ Server Actions เพื่อความปลอดภัย เข้าถึงคุกกี้ และรัน RLS Logic (`lib/supabase/server.ts`)
- **Direct Fetch:** ในบางกรณีมีการใช้ Native `fetch` เพื่อเรียก RPC หรือ REST API ของ Supabase โดยตรงเพื่อประสิทธิภาพสูงสุด

---

## 7. Utils Layer
รวมฟังก์ชันช่วยเหลือที่ใช้ซ้ำบ่อย:
- **`formatThaiDate.ts`:** จัดการแปลงวันที่ ISO เป็นรูปแบบภาษาไทย พุทธศักราช และเวลาที่อ่านง่าย
- **`permissions.ts`:** จัดการตรวจสอบสิทธิ์ของผู้ใช้ (Role-based access control)
- **`docxGenerator.ts`:** ส่วนงาน Generate เอกสาร Word (ใบยืม-คืน) จาก Template
- **`utils.ts`:** ฟังก์ชันทั่วไป เช่น `cn` สำหรับการ Merge Tailwind classes

---

## 8. API Endpoint
ใน V4 ระบบเปลี่ยนมาใช้ **Next.js Server Actions** แทน API Route แบบดั้งเดิมเกือบทั้งหมด เพื่อลดความซับซ้อน:
- `submitReservationRequest`: สำหรับสร้างคำขอจอง
- `convertReservationToLoanAction`: แปลงสถานะจากการจองเป็นการยืมจริง
- `deleteUser`: ลบข้อมูลผู้ใช้พร้อมจัดการ Referential Integrity
- `updateEquipmentStatus`: อัปเดตสถานะความพร้อมของอุปกรณ์

---

## 9. ตัวอย่างจริง (Real Example)
ตัวอย่างโครงสร้างของ **Server Action** สำหรับการจองอุปกรณ์:
```typescript
// app/reservations/actions.ts
'use server'

export async function submitReservationRequest(formData: FormData) {
    const supabase = await createClient()
    const { data: { user } } = await supabase.auth.getUser()
    
    // 1. Validation Logic
    if (!user) return { error: 'กรุณาเข้าสู่ระบบ' }

    // 2. Database Operation
    const { data, error } = await supabase
        .from('reservations')
        .insert({ 
            user_id: user.id, 
            equipment_id: formData.get('equipmentId'),
            // ... dates
        })

    // 3. Notification
    if (!error) {
        await sendDiscordNotification('New reservation created!')
    }
    
    // 4. Cache Management
    revalidatePath('/my-reservations')
    return { success: true }
}
```

---

## 10. Security
ระบบรักษาความปลอดภัยผ่าน 3 ชั้นหลัก:
1. **Middleware:** ตรวจสอบ Session JWT ของ Supabase ก่อนเข้าถึงหน้าที่มีการป้องกัน
2. **Row Level Security (RLS):** นโยบายความปลอดภัยในระดับตารางฐานข้อมูล (PostgreSQL) เพื่อให้แน่ใจว่าผู้ใช้เห็นและแก้ไขได้เฉพาะข้อมูลที่ตนมีสิทธิ์เท่านั้น
3. **Server-Side Validation:** ตรวจสอบความถูกต้องของข้อมูล (Input Type & Logic) ภายใต้ Server Actions เสมอ

---

## 11. Best Practices
- **Server-First:** เน้นการประมวลผลที่ Server โดยใช้ Server Components เพื่อลดภาระของ Client
- **Atomic Operations:** ใช้ Transaction หรือ RPC เมื่อต้องจัดการข้อมูลหลายตารางพร้อมกัน
- **Error Boundaries:** มีการดักจับ Error และแจ้งเตือนผู้ใช้งานด้วย UI ที่เข้าใจง่าย (Toast Notifications)
- **Clean Audit Trail:** ทุกการเปลี่ยนแปลงสถานะสำคัญ ต้องมีการบันทึกใน Activity Log เสมอ
- **Typed Data:** ใช้ TypeScript Interface ที่ Generate จากฐานข้อมูลเพื่อให้โค้ดมีความแม่นยำสูง

---
*เอกสารนี้จัดทำขึ้นเพื่อให้ทีมนักพัฒนาเข้าใจโครงสร้างและมาตรฐานของระบบ พัฒนาโดย Antigravity.*
