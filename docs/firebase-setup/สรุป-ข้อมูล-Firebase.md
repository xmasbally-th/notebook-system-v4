# สรุปข้อมูลใน Firebase - ระบบยืม-คืนอุปกรณ์

## 📊 สถานะปัจจุบัน

### ✅ Collections ที่มีอยู่แล้ว (5 collections)
1. **settings** - ตั้งค่าระบบทั่วไป
2. **systemSettings** - ตั้งค่าระบบหลัก
3. **equipmentCategories** - หมวดหมู่อุปกรณ์
4. **equipmentManagement** - จัดการอุปกรณ์
5. **users** - ข้อมูลผู้ใช้

### ❌ Collections ที่ยังขาด (7 collections สำคัญ)

#### 🔴 ความสำคัญสูงสุด (ต้องมีก่อนใช้งานจริง)
1. **loanRequests** - บันทึกคำขอยืมอุปกรณ์
   - เก็บข้อมูล: ผู้ยืม, อุปกรณ์, วันที่ยืม-คืน, สถานะ
   - สถานะ: pending, approved, rejected, borrowed, returned, overdue

2. **reservations** - บันทึกการจองอุปกรณ์ล่วงหน้า
   - เก็บข้อมูล: ผู้จอง, อุปกรณ์, วันเวลาที่จอง, วัตถุประสงค์
   - สถานะ: pending, approved, ready, completed, cancelled, expired

3. **notifications** - การแจ้งเตือนส่วนตัว
   - เก็บข้อมูล: ผู้รับ, ประเภท, ข้อความ, สถานะอ่าน
   - ประเภท: loan_request, loan_approved, loan_reminder, reservation_reminder

#### 🟡 ความสำคัญปานกลาง (ควรมี)
4. **notificationSettings** - ตั้งค่าการแจ้งเตือนของแต่ละคน
5. **activityLogs** - บันทึกกิจกรรมทั้งหมด (Audit Trail)
6. **publicStats** - สถิติสาธารณะสำหรับหน้าแรก

#### 🟢 ความสำคัญต่ำ (เพิ่มทีหลังได้)
7. **scheduledNotifications** - การแจ้งเตือนที่ต้องส่งในอนาคต
8. **closedDates** - วันที่ปิดให้บริการ
9. **categoryLimits** - จำกัดจำนวนการยืมตามหมวดหมู่
10. **settingsAuditLog** - บันทึกการเปลี่ยนแปลงการตั้งค่า

---

## 🎯 สิ่งที่ต้องทำ

### ขั้นตอนที่ 1: สร้าง Collections ที่จำเป็น

#### วิธีที่ 1: ใช้ Script (แนะนำ - รวดเร็ว)
```bash
# รัน script เพื่อสร้าง collections พร้อมข้อมูลตัวอย่าง
node scripts/initialize-core-collections.js
```

Script นี้จะสร้าง:
- ✅ publicStats (พร้อมข้อมูลเริ่มต้น)
- ✅ closedDates (พร้อมวันหยุดตัวอย่าง)
- ✅ categoryLimits (สำหรับทุกหมวดหมู่ที่มี)
- ✅ loanRequests (พร้อมคำขอยืมตัวอย่าง)
- ✅ reservations (พร้อมการจองตัวอย่าง)
- ✅ notifications (พร้อมการแจ้งเตือนตัวอย่าง)
- ✅ notificationSettings (สำหรับ admin)

#### วิธีที่ 2: สร้างผ่าน Firebase Console (ทำเอง)
1. เข้า Firebase Console → Firestore Database
2. คลิก "Start collection"
3. ใส่ชื่อ collection ตามรายการด้านบน
4. เพิ่ม document แรก (ใส่ข้อมูลตามโครงสร้างที่กำหนด)

#### วิธีที่ 3: ปล่อยให้สร้างอัตโนมัติ
- Collections จะถูกสร้างอัตโนมัติเมื่อมีการใช้งานครั้งแรก
- แต่ต้องแน่ใจว่า Security Rules พร้อมแล้ว

---

### ขั้นตอนที่ 2: ตรวจสอบ Security Rules

ตรวจสอบว่าไฟล์ `firestore.rules` มี rules สำหรับทุก collection แล้ว:

```bash
# ตรวจสอบ rules
cat firestore.rules

# Deploy rules
firebase deploy --only firestore:rules
```

---

### ขั้นตอนที่ 3: Deploy Indexes

```bash
# ตรวจสอบ indexes
cat firestore.indexes.json

# Deploy indexes
firebase deploy --only firestore:indexes
```

---

## 📋 โครงสร้างข้อมูลที่สำคัญ

### 1. loanRequests (คำขอยืม)
```javascript
{
  equipmentId: "eq123",           // ID อุปกรณ์
  equipmentName: "MacBook Pro",   // ชื่ออุปกรณ์
  userId: "user123",              // ID ผู้ยืม
  userName: "สมชาย ใจดี",         // ชื่อผู้ยืม
  userEmail: "somchai@g.lpru.ac.th",
  borrowDate: Timestamp,          // วันที่ยืม
  expectedReturnDate: Timestamp,  // วันที่ต้องคืน
  actualReturnDate: Timestamp,    // วันที่คืนจริง (ถ้ามี)
  purpose: "ใช้ทำโปรเจค",         // วัตถุประสงค์
  notes: "ต้องการใช้ 1 สัปดาห์",  // หมายเหตุ
  status: "pending",              // สถานะ
  createdAt: Timestamp,
  updatedAt: Timestamp
}
```

**สถานะที่เป็นไปได้:**
- `pending` - รอการอนุมัติ
- `approved` - อนุมัติแล้ว
- `rejected` - ปฏิเสธ
- `borrowed` - กำลังยืมอยู่
- `returned` - คืนแล้ว
- `overdue` - เกินกำหนดคืน

### 2. reservations (การจอง)
```javascript
{
  equipmentId: "eq123",
  equipmentName: "MacBook Pro",
  userId: "user123",
  userName: "สมชาย ใจดี",
  startTime: Timestamp,           // เวลาเริ่มต้น
  endTime: Timestamp,             // เวลาสิ้นสุด
  purpose: "ใช้สอน",
  status: "pending",
  notificationSent: false,        // ส่งการแจ้งเตือนแล้วหรือไม่
  createdAt: Timestamp
}
```

**สถานะที่เป็นไปได้:**
- `pending` - รอการอนุมัติ
- `approved` - อนุมัติแล้ว
- `ready` - พร้อมรับอุปกรณ์
- `completed` - เสร็จสิ้น
- `cancelled` - ยกเลิก
- `expired` - หมดอายุ (ไม่มารับ)

### 3. notifications (การแจ้งเตือน)
```javascript
{
  userId: "user123",              // ผู้รับการแจ้งเตือน
  type: "loan_approved",          // ประเภท
  title: "คำขอยืมได้รับการอนุมัติ",
  message: "คำขอยืม MacBook Pro ของคุณได้รับการอนุมัติแล้ว",
  data: {                         // ข้อมูลเพิ่มเติม
    loanId: "loan123",
    equipmentId: "eq123"
  },
  isRead: false,                  // อ่านแล้วหรือไม่
  priority: "high",               // ความสำคัญ
  actionUrl: "/my-loans/loan123", // URL สำหรับดูรายละเอียด
  actionText: "ดูรายละเอียด",
  createdAt: Timestamp
}
```

**ประเภทการแจ้งเตือน:**
- `loan_request` - มีคำขอยืมใหม่ (สำหรับ admin)
- `loan_approved` - คำขอยืมได้รับการอนุมัติ
- `loan_rejected` - คำขอยืมถูกปฏิเสธ
- `loan_reminder` - แจ้งเตือนคืนอุปกรณ์
- `reservation_reminder` - แจ้งเตือนการจอง
- `user_approval` - ผู้ใช้ใหม่รอการอนุมัติ (สำหรับ admin)
- `equipment_returned` - อุปกรณ์ถูกคืนแล้ว

---

## 🔍 การตรวจสอบข้อมูล

### ตรวจสอบว่า Collections ถูกสร้างแล้ว
1. เข้า Firebase Console
2. ไปที่ Firestore Database
3. ดูว่ามี collections ตามรายการหรือไม่

### ตรวจสอบจำนวนข้อมูล
```javascript
// ใน Firebase Console > Firestore
// คลิกที่แต่ละ collection เพื่อดูจำนวน documents
```

---

## 💡 คำแนะนำ

### สำหรับการพัฒนา (Development)
1. ใช้ Firebase Emulator เพื่อทดสอบก่อน
2. สร้างข้อมูลตัวอย่างเพื่อทดสอบ UI
3. ทดสอบ Security Rules ให้ครบทุก case

### สำหรับการใช้งานจริง (Production)
1. ตรวจสอบ Security Rules ให้แน่ใจว่าปลอดภัย
2. สร้าง Backup policy
3. ตั้งค่า Monitoring และ Alerts
4. ทดสอบ Performance ก่อน deploy

---

## 📞 ติดต่อ/ช่วยเหลือ

หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ Firebase Console Logs
2. ดู Error messages ใน Browser Console
3. ตรวจสอบ Security Rules
4. ตรวจสอบ Network tab ว่ามี API errors หรือไม่

---

## ✅ Checklist ก่อนใช้งานจริง

- [ ] สร้าง collections ที่จำเป็นทั้งหมด
- [ ] ตรวจสอบ Security Rules
- [ ] Deploy Indexes
- [ ] ทดสอบการยืม-คืนอุปกรณ์
- [ ] ทดสอบการจองอุปกรณ์
- [ ] ทดสอบระบบการแจ้งเตือน
- [ ] ทดสอบ User approval flow
- [ ] สร้างข้อมูลอุปกรณ์จริง
- [ ] สร้าง Admin user
- [ ] ทดสอบบน Production environment

---

**อัปเดตล่าสุด:** 20 พฤศจิกายน 2025
