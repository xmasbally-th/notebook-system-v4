# คู่มือการจัดการหมวดหมู่อุปกรณ์

## ✅ ระบบมีหน้าจัดการหมวดหมู่แล้ว!

ระบบมีหน้า **Category Management** สำหรับ Admin ในการจัดการหมวดหมู่อุปกรณ์

## การเข้าถึง

### URL
```
/admin/categories
```

### สิทธิ์การเข้าถึง
- ✅ เฉพาะ Admin เท่านั้น
- ✅ มี ProtectedRoute ป้องกันแล้ว

## ฟีเจอร์ที่มี

### 1. Seed Categories (เพิ่มข้อมูลเริ่มต้น)
หน้า CategoryManagement มีปุ่มสำหรับเพิ่มหมวดหมู่เริ่มต้น:

**หมวดหมู่หลัก (5 หมวดหมู่):**
1. 💻 คอมพิวเตอร์และอุปกรณ์
2. 🎬 อุปกรณ์โสตทัศนูปกรณ์
3. 🖨️ เครื่องใช้สำนักงาน
4. 🔧 เครื่องมือและอุปกรณ์
5. 🪑 เฟอร์นิเจอร์

**หมวดหมู่ย่อย:**
- คอมพิวเตอร์: Desktop, Laptop, Monitor, Printer
- โสตทัศนูปกรณ์: Projector, Camera, Audio Equipment

### 2. เพิ่มหมวดหมู่ใหม่
- สามารถเพิ่มหมวดหมู่หลักและหมวดหมู่ย่อยได้
- กำหนด icon, สี, คำอธิบาย
- รองรับทั้งภาษาไทยและอังกฤษ

### 3. แก้ไขหมวดหมู่
- แก้ไขชื่อ, คำอธิบาย
- เปลี่ยน icon และสี
- จัดเรียงลำดับ

### 4. ลบหมวดหมู่
- ลบหมวดหมู่ที่ไม่ใช้งาน
- ตรวจสอบว่ามีอุปกรณ์ในหมวดหมู่หรือไม่

## Collection ที่ใช้

```javascript
Collection: 'equipmentCategories'
```

**โครงสร้างข้อมูล:**
```javascript
{
  name: 'คอมพิวเตอร์และอุปกรณ์',
  nameEn: 'Computers & Equipment',
  description: 'อุปกรณ์คอมพิวเตอร์และเทคโนโลยีสารสนเทศ',
  icon: '💻',
  color: '#3B82F6',
  level: 0,              // 0 = หมวดหมู่หลัก, 1 = หมวดหมู่ย่อย
  sortOrder: 1,
  parentId: null,        // สำหรับหมวดหมู่ย่อย
  equipmentCount: 0,
  isActive: true,
  createdAt: Timestamp,
  createdBy: 'userId'
}
```

## วิธีใช้งาน

### ขั้นตอนที่ 1: เข้าหน้า Category Management
1. Login ด้วย Admin account
2. ไปที่เมนู **Admin** > **Category Management**
3. หรือเข้าผ่าน URL: `/admin/categories`

### ขั้นตอนที่ 2: เพิ่มหมวดหมู่เริ่มต้น
1. คลิกปุ่ม **"Seed Categories"** หรือ **"เพิ่มข้อมูลเริ่มต้น"**
2. รอระบบสร้างหมวดหมู่ (ประมาณ 5-10 วินาที)
3. ตรวจสอบผลลัพธ์ที่แสดง

### ขั้นตอนที่ 3: ตรวจสอบหมวดหมู่
1. ดูรายการหมวดหมู่ที่สร้างแล้ว
2. ตรวจสอบว่ามีหมวดหมู่ครบถ้วน
3. ทดสอบเพิ่ม/แก้ไข/ลบหมวดหมู่

### ขั้นตอนที่ 4: กลับไปตั้งค่า Category Limits
1. ไปที่ **Admin** > **Settings** > **Category Limits**
2. ตอนนี้จะเห็นรายการหมวดหมู่แล้ว
3. ตั้งค่าจำนวนจำกัดสำหรับแต่ละหมวดหมู่

## การแก้ปัญหา

### ปัญหา: ไม่เห็นปุ่ม Seed Categories
**แก้ไข:**
- ตรวจสอบว่า login ด้วย Admin account
- Refresh หน้าเว็บ
- ตรวจสอบ Console (F12) ว่ามี error หรือไม่

### ปัญหา: กดปุ่มแล้วไม่มีอะไรเกิดขึ้น
**แก้ไข:**
- เปิด Console (F12) ดู error
- ตรวจสอบ Firestore Rules ว่าอนุญาตให้ Admin เขียนข้อมูลได้
- ตรวจสอบ Firebase connection

### ปัญหา: หมวดหมู่ถูกสร้างแล้วแต่ไม่แสดงใน Category Limits
**แก้ไข:**
1. Refresh หน้า Category Limits
2. ตรวจสอบว่าใช้ collection name ถูกต้อง (`equipmentCategories`)
3. ตรวจสอบ EquipmentCategoriesContext ว่าโหลดข้อมูลถูกต้อง

## Firestore Rules ที่จำเป็น

```javascript
// ใน firestore.rules
match /equipmentCategories/{categoryId} {
  // อนุญาตให้ทุกคนอ่านได้
  allow read: if true;
  
  // เฉพาะ admin เท่านั้นที่แก้ไขได้
  allow create, update, delete: if isAdmin();
}

function isAdmin() {
  return request.auth != null && 
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
}
```

## ไฟล์ที่เกี่ยวข้อง

- `src/components/admin/CategoryManagement.js` - หน้าจัดการหมวดหมู่
- `src/services/equipmentCategoryService.js` - Service สำหรับจัดการหมวดหมู่
- `src/contexts/EquipmentCategoriesContext.js` - Context สำหรับแชร์ข้อมูล
- `src/hooks/useEquipmentCategories.js` - Hook สำหรับโหลดข้อมูล
- `src/App.js` - Route definition

## สรุป

✅ **ระบบมีหน้าจัดการหมวดหมู่แล้ว**
- URL: `/admin/categories`
- มีฟังก์ชัน Seed Categories
- สามารถเพิ่ม/แก้ไข/ลบหมวดหมู่ได้
- รองรับหมวดหมู่หลักและหมวดหมู่ย่อย

✅ **วิธีแก้ปัญหา Category Limits ไม่มีข้อมูล:**
1. เข้าไปที่ `/admin/categories`
2. คลิกปุ่ม "Seed Categories"
3. รอระบบสร้างหมวดหมู่
4. กลับไปที่ Category Limits จะเห็นข้อมูลแล้ว

## หมายเหตุ

- ต้องสร้างหมวดหมู่ก่อนถึงจะใช้งาน Category Limits ได้
- แนะนำให้ใช้ฟังก์ชัน Seed Categories เพื่อสร้างข้อมูลเริ่มต้น
- สามารถเพิ่มหมวดหมู่เองได้ตามต้องการ
- หมวดหมู่ที่สร้างจะใช้ได้ทันทีในทุกส่วนของระบบ
