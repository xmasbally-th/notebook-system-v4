# ✅ Admin Setup Checklist

## การตั้งค่า Admin คนแรก - Checklist

ใช้ checklist นี้เพื่อตรวจสอบว่าคุณทำครบทุกขั้นตอนแล้ว

---

### 📋 ก่อนเริ่ม

- [ ] มี Firebase project แล้ว
- [ ] มี user account ที่ต้องการตั้งเป็น admin
- [ ] เข้าถึง Firebase Console ได้
- [ ] รู้ email ของ user ที่ต้องการตั้งเป็น admin

---

### 🔧 ขั้นตอนการตั้งค่า

#### 1. เข้า Firebase Console
- [ ] เปิด https://console.firebase.google.com
- [ ] เลือกโปรเจค Equipment Lending System
- [ ] คลิกเมนู "Firestore Database"

#### 2. ตรวจสอบ Users Collection
- [ ] เห็น collection ชื่อ "users"
- [ ] เห็น document ของ user ที่ต้องการตั้งเป็น admin
- [ ] ตรวจสอบว่า email ตรงกับที่ต้องการ

#### 3. แก้ไข User Document
- [ ] คลิกที่ user document
- [ ] เห็นฟิลด์ "role" และ "status"
- [ ] แก้ไข `role` จาก `"user"` เป็น `"admin"`
- [ ] แก้ไข `status` เป็น `"approved"`
- [ ] คลิกปุ่ม "Update" หรือ "Save"
- [ ] เห็นข้อความยืนยันว่าบันทึกสำเร็จ

#### 4. ตรวจสอบ Settings Collection
- [ ] กลับไปหน้า Firestore Database
- [ ] มองหา collection ชื่อ "settings"

**ถ้ามี settings collection:**
- [ ] เปิด collection "settings"
- [ ] เห็น document ชื่อ "systemSettings"
- [ ] ข้ามไปขั้นตอนที่ 5

**ถ้าไม่มี settings collection:**
- [ ] คลิก "Start collection"
- [ ] ใส่ Collection ID: `settings`
- [ ] ใส่ Document ID: `systemSettings`
- [ ] เพิ่มฟิลด์ทั้งหมดตามตารางด้านล่าง
- [ ] คลิก "Save"

**ฟิลด์ที่ต้องเพิ่ม:**
- [ ] `maxLoanDuration` (number) = `7`
- [ ] `maxAdvanceBookingDays` (number) = `30`
- [ ] `defaultCategoryLimit` (number) = `3`
- [ ] `discordEnabled` (boolean) = `false`
- [ ] `discordWebhookUrl` (string) = `""` (ว่าง)
- [ ] `lastUpdated` (timestamp) = ใช้ "Use server timestamp"
- [ ] `lastUpdatedBy` (string) = `"system"`

#### 5. ทดสอบบนเว็บไซต์
- [ ] ไปที่เว็บไซต์
- [ ] คลิก "Sign Out" (ออกจากระบบ)
- [ ] ล้าง browser cache:
  - [ ] กด Ctrl+Shift+Delete (Windows) หรือ Cmd+Shift+Delete (Mac)
  - [ ] เลือก "Cached images and files"
  - [ ] คลิก "Clear data"
- [ ] ปิด browser
- [ ] เปิด browser ใหม่
- [ ] เข้าสู่ระบบอีกครั้ง
- [ ] ไปที่หน้า Admin Settings (`/admin/settings`)

#### 6. ตรวจสอบผลลัพธ์
- [ ] เห็นหน้า Admin Settings (ไม่มี error)
- [ ] เห็นแท็บต่างๆ: "ทั่วไป", "กฎการยืม", "วันปิดทำการ", ฯลฯ
- [ ] สามารถคลิกแท็บต่างๆ ได้
- [ ] ไม่เห็นข้อความ "Missing or insufficient permissions"

---

### 🎯 ผลลัพธ์ที่คาดหวัง

เมื่อทำครบทุกขั้นตอน คุณควรจะ:
- ✅ เข้าหน้า Admin Settings ได้
- ✅ เห็นข้อมูลการตั้งค่าต่างๆ
- ✅ สามารถแก้ไขการตั้งค่าได้
- ✅ เห็นเมนู Admin ในแถบนำทาง

---

### 🐛 ถ้ายังมีปัญหา

#### ปัญหา: ยังเห็น error "Missing or insufficient permissions"

**ตรวจสอบ:**
- [ ] แก้ไข `role` เป็น `"admin"` แล้ว (ไม่ใช่ `admin` ไม่มีเครื่องหมายคำพูด)
- [ ] แก้ไข `status` เป็น `"approved"` แล้ว
- [ ] กดปุ่ม "Update" หรือ "Save" แล้ว
- [ ] ออกจากระบบและเข้าใหม่แล้ว
- [ ] ล้าง cache แล้ว

**ลองทำ:**
- [ ] Hard refresh หน้าเว็บ (Ctrl+Shift+R หรือ Cmd+Shift+R)
- [ ] ลองใช้ browser อื่น
- [ ] ลองใช้ Incognito/Private mode

#### ปัญหา: ไม่เจอ collection "users"

**สาเหตุ:** ยังไม่มี user ในระบบ

**แก้ไข:**
- [ ] ไปที่เว็บไซต์
- [ ] ลอง login ด้วย Google
- [ ] กลับมาดูใน Firestore อีกครั้ง

#### ปัญหา: ไม่สามารถแก้ไข document ได้

**สาเหตุ:** ไม่มีสิทธิ์ในการแก้ไข Firestore

**แก้ไข:**
- [ ] ตรวจสอบว่าคุณเป็น Owner หรือ Editor ของ Firebase project
- [ ] ลองใช้ account อื่นที่มีสิทธิ์

---

### 📝 หมายเหตุ

- ⏱️ ใช้เวลาประมาณ 5-10 นาที
- 🔒 ทำเพียงครั้งเดียวสำหรับ admin คนแรก
- ✅ Admin คนต่อไปตั้งค่าผ่านหน้า User Management
- 📊 การเปลี่ยนแปลงมีผลทันทีหลัง login ใหม่

---

### 🔗 เอกสารที่เกี่ยวข้อง

- [Quick Fix Guide](QUICK_FIX_ADMIN_SETTINGS.md) - คำแนะนำแบบย่อ
- [Detailed Fix Guide](ADMIN_SETTINGS_PERMISSION_FIX.md) - คำแนะนำแบบละเอียด
- [Admin Settings Guide](docs/admin-settings-guide.md) - คู่มือการใช้งาน

---

### ✨ เสร็จสิ้น!

เมื่อทำครบทุกขั้นตอนแล้ว คุณพร้อมใช้งานระบบ Admin Settings แล้ว! 🎉

**ขั้นตอนถัดไป:**
1. ทดลองแก้ไขการตั้งค่าต่างๆ
2. เพิ่ม admin คนอื่นผ่านหน้า User Management
3. ตั้งค่าระบบตามความต้องการ

---

**หากยังมีปัญหา กรุณาติดต่อทีมพัฒนา** 🙏
