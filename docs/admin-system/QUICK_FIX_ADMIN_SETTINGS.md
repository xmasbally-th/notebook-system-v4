# 🚀 แก้ไขปัญหา Admin Settings ด่วน (5 นาที)

## ❌ ปัญหา
หน้า `/admin/settings` แสดงข้อความ:
```
เกิดข้อผิดพลาดในการโหลดการตั้งค่า
Missing or insufficient permissions.
```

## ✅ วิธีแก้ไข (ทำตามขั้นตอน)

### ขั้นตอนที่ 1: เข้า Firebase Console

1. เปิด browser ไปที่ https://console.firebase.google.com
2. เลือกโปรเจค **Equipment Lending System**
3. คลิกที่เมนู **Firestore Database** ทางซ้ายมือ

### ขั้นตอนที่ 2: ตั้งค่า Admin

1. ใน Firestore Database คลิกที่ collection **`users`**
2. คุณจะเห็นรายการ user documents (แต่ละ document คือ 1 user)
3. **หา document ของคุณ** โดยดูจาก email ที่คุณใช้ login
4. คลิกที่ document นั้น
5. คุณจะเห็นฟิลด์ต่างๆ เช่น:
   ```
   email: "your-email@example.com"
   displayName: "Your Name"
   role: "user"          ← ต้องแก้ตรงนี้
   status: "incomplete"  ← และตรงนี้
   ```

6. **แก้ไขฟิลด์ `role`:**
   - คลิกที่ค่า `"user"`
   - เปลี่ยนเป็น `"admin"`
   - กด Enter

7. **แก้ไขฟิลด์ `status`:**
   - คลิกที่ค่า `"incomplete"` หรือ `"pending"`
   - เปลี่ยนเป็น `"approved"`
   - กด Enter

8. คลิกปุ่ม **"Update"** หรือ **"Save"** ด้านบน

### ขั้นตอนที่ 3: สร้าง Settings Document (ถ้ายังไม่มี)

1. กลับไปที่หน้า Firestore Database
2. ดูว่ามี collection **`settings`** หรือไม่
3. **ถ้ายังไม่มี** ให้สร้างใหม่:
   - คลิก **"Start collection"**
   - Collection ID: `settings`
   - Document ID: `systemSettings`
   - เพิ่มฟิลด์ต่อไปนี้:

   | Field | Type | Value |
   |-------|------|-------|
   | `maxLoanDuration` | number | `7` |
   | `maxAdvanceBookingDays` | number | `30` |
   | `defaultCategoryLimit` | number | `3` |
   | `discordEnabled` | boolean | `false` |
   | `discordWebhookUrl` | string | `""` (ว่าง) |
   | `lastUpdated` | timestamp | (คลิก "Use server timestamp") |
   | `lastUpdatedBy` | string | `"system"` |

4. คลิก **"Save"**

### ขั้นตอนที่ 4: ทดสอบ

1. **ออกจากระบบ** บนเว็บไซต์ (Sign Out)
2. **ล้าง cache** ของ browser:
   - กด `Ctrl + Shift + Delete` (Windows)
   - กด `Cmd + Shift + Delete` (Mac)
   - เลือก "Cached images and files"
   - คลิก "Clear data"
3. **ปิดและเปิด browser ใหม่**
4. **เข้าสู่ระบบอีกครั้ง**
5. ไปที่หน้า **Admin Settings** (`/admin/settings`)
6. ควรเห็นหน้าการตั้งค่าแทนข้อความ error

---

## 🎯 ผลลัพธ์ที่คาดหวัง

หลังจากทำตามขั้นตอนแล้ว คุณควรจะ:
- ✅ เข้าหน้า Admin Settings ได้
- ✅ เห็นแท็บต่างๆ เช่น "ทั่วไป", "กฎการยืม", "วันปิดทำการ"
- ✅ สามารถแก้ไขการตั้งค่าได้

---

## 🐛 ถ้ายังมีปัญหา

### ปัญหา 1: ยังเห็น error เหมือนเดิม

**วิธีแก้:**
1. ตรวจสอบว่าแก้ไข `role` และ `status` ถูกต้องแล้ว
2. ตรวจสอบว่ากด "Update" หรือ "Save" แล้ว
3. ลอง **Hard Refresh** หน้าเว็บ:
   - กด `Ctrl + Shift + R` (Windows)
   - กด `Cmd + Shift + R` (Mac)

### ปัญหา 2: ไม่เจอ collection `users`

**สาเหตุ:** ยังไม่มี user ในระบบ

**วิธีแก้:**
1. ไปที่เว็บไซต์
2. ลอง login ด้วย Google
3. กลับมาดูใน Firestore อีกครั้ง

### ปัญหา 3: แก้แล้วแต่ยังเข้าไม่ได้

**ตรวจสอบ:**
1. เปิด Developer Console ของ browser (กด F12)
2. ไปที่แท็บ "Console"
3. ดูว่ามี error message อะไร
4. ถ่ายภาพหน้าจอและส่งให้ทีมพัฒนา

---

## 📸 ภาพประกอบ (ตัวอย่าง)

### ก่อนแก้ไข:
```json
{
  "email": "admin@example.com",
  "displayName": "Admin User",
  "role": "user",        ← ต้องเปลี่ยน
  "status": "incomplete" ← ต้องเปลี่ยน
}
```

### หลังแก้ไข:
```json
{
  "email": "admin@example.com",
  "displayName": "Admin User",
  "role": "admin",    ← เปลี่ยนแล้ว ✅
  "status": "approved" ← เปลี่ยนแล้ว ✅
}
```

---

## 📝 หมายเหตุสำคัญ

- ⚠️ ทำการตั้งค่า admin **เพียงครั้งเดียว** สำหรับ admin คนแรก
- ✅ Admin คนต่อไปสามารถตั้งค่าผ่านหน้า **User Management** ได้
- 🔒 อย่าตั้งค่า admin ให้กับ user ที่ไม่น่าเชื่อถือ
- 📊 การเปลี่ยนแปลงจะมีผลทันทีหลังจาก login ใหม่

---

## 🔗 เอกสารเพิ่มเติม

- [Admin Settings Permission Fix (ฉบับเต็ม)](ADMIN_SETTINGS_PERMISSION_FIX.md)
- [Admin Settings Guide](docs/admin-settings-guide.md)
- [Firestore Security Rules](firestore.rules)

---

## ⏱️ เวลาที่ใช้

- ขั้นตอนที่ 1-2: **2 นาที**
- ขั้นตอนที่ 3: **2 นาที** (ถ้าต้องสร้าง settings)
- ขั้นตอนที่ 4: **1 นาที**
- **รวม: 5 นาที**

---

**หากทำตามขั้นตอนแล้วยังมีปัญหา กรุณาติดต่อทีมพัฒนา** 🙏
