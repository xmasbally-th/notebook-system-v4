# แก้ไขโครงสร้างข้อมูล Equipment Management

## 🎯 วัตถุประสงค์
แก้ไขข้อมูลใน collection `equipmentManagement` ให้ตรงตาม schema ที่ถูกต้อง เพื่อให้หน้าจัดการอุปกรณ์ใช้งานได้

## ❌ ปัญหาที่พบ

จากข้อมูลปัจจุบันใน Firestore:

1. **status ผิด**: ใช้ `"available"` แทนที่จะเป็น `"active"`, `"maintenance"`, `"retired"`, หรือ `"lost"`

2. **ขาดฟิลด์สำคัญ**:
   - `location` (object) - ข้อมูลสถานที่
   - `responsiblePerson` (object) - ผู้รับผิดชอบ
   - `specifications` (object) - ข้อมูลจำเพาะ
   - `brand`, `model`, `description` - ข้อมูลพื้นฐาน
   - `purchaseDate`, `purchasePrice`, `vendor`, `warrantyExpiry` - ข้อมูลการซื้อ

3. **โครงสร้าง category ไม่สมบูรณ์**: ต้องมี `id`, `name`, `icon`

4. **images เป็น array ว่าง**: ควรมี object ที่มี `url`, `thumbnailUrl`, `filename`, etc.

## 🔧 วิธีแก้ไข

### ขั้นตอนที่ 1: ตรวจสอบข้อมูลปัจจุบัน

```bash
node scripts/validate-equipment-schema.js
```

สคริปต์นี้จะ:
- ตรวจสอบว่าข้อมูลครบถ้วนหรือไม่
- แสดงรายการฟิลด์ที่ขาดหายไป
- แสดงค่าที่ไม่ถูกต้อง

### ขั้นตอนที่ 2: แก้ไขข้อมูล

```bash
node scripts/fix-equipment-schema.js
```

สคริปต์นี้จะ:
- แก้ไข `status` จาก `"available"` เป็น `"active"`
- เพิ่มฟิลด์ที่ขาดหายไป
- แก้ไขโครงสร้าง `category`, `location`, `responsiblePerson`
- อัปเดต `searchKeywords`
- เพิ่ม `version` และ `updatedAt`

### ขั้นตอนที่ 3: ตรวจสอบอีกครั้ง

```bash
node scripts/validate-equipment-schema.js
```

ตรวจสอบว่าข้อมูลถูกต้องแล้ว

## 📋 โครงสร้างที่ถูกต้อง

```javascript
{
  // ข้อมูลพื้นฐาน (Required)
  equipmentNumber: "EQ-001",
  name: "โน้ตบุ๊ค Acer",
  
  // Category (Required)
  category: {
    id: "computers",
    name: "คอมพิวเตอร์",
    icon: "💻"
  },
  
  // ข้อมูลเพิ่มเติม
  brand: "Acer",
  model: "Aspire 5",
  description: "โน้ตบุ๊คสำหรับงานทั่วไป",
  specifications: {
    cpu: "Intel i5",
    ram: "8GB",
    storage: "256GB SSD"
  },
  
  // Status (Required) - ต้องเป็น active, maintenance, retired, หรือ lost
  status: "active",
  
  // Location (Required)
  location: {
    building: "อาคาร 1",
    floor: "ชั้น 3",
    room: "ห้อง 301",
    description: "ห้องคอมพิวเตอร์"
  },
  
  // ข้อมูลการซื้อ
  purchaseDate: Timestamp,
  purchasePrice: 25000,
  vendor: "บริษัท ABC",
  warrantyExpiry: Timestamp,
  
  // Responsible Person (Required)
  responsiblePerson: {
    uid: "user123",
    name: "สมชาย ใจดี",
    email: "somchai@example.com",
    department: "IT"
  },
  
  // รูปภาพ
  images: [
    {
      id: "img1",
      url: "https://...",
      thumbnailUrl: "https://...",
      filename: "laptop.jpg",
      size: 1024000,
      uploadedAt: Timestamp,
      uploadedBy: "user123"
    }
  ],
  
  // QR Code
  qrCode: {
    url: "https://...",
    generatedAt: Timestamp
  },
  
  // Tags และ Search
  tags: ["laptop", "computer"],
  searchKeywords: ["eq-001", "โน้ตบุ๊ค", "acer"],
  notes: "หมายเหตุเพิ่มเติม",
  
  // Metadata (Required)
  createdAt: Timestamp,
  createdBy: "user123",
  updatedAt: Timestamp,
  updatedBy: "user123",
  version: 1,
  isActive: true,
  viewCount: 0,
  lastViewed: null
}
```

## ✅ การทดสอบหลังแก้ไข

1. **ตรวจสอบใน Firebase Console**:
   - เปิด Firestore
   - ดู collection `equipmentManagement`
   - ตรวจสอบว่าฟิลด์ครบถ้วน

2. **ทดสอบในแอป**:
   - เข้าหน้าจัดการอุปกรณ์
   - ตรวจสอบว่าแสดงข้อมูลได้
   - ลองค้นหาและกรองข้อมูล
   - ลองแก้ไขข้อมูล

3. **ตรวจสอบ Console**:
   - เปิด Developer Tools
   - ดู Console ว่ามี error หรือไม่
   - ตรวจสอบ Network tab

## 🚨 หมายเหตุสำคัญ

1. **Backup ข้อมูล**: สคริปต์จะใช้ `merge: true` เพื่อไม่ลบข้อมูลเดิม
2. **Service Account Key**: ต้องมีไฟล์ `serviceAccountKey.json` ในโฟลเดอร์ root
3. **Status Mapping**: 
   - `available` → `active`
   - `in_use` → `active`
   - `unavailable` → `maintenance`
   - `broken` → `maintenance`
4. **Default Values**: ฟิลด์ที่ไม่มีจะถูกเติมค่า default

## 📞 หากมีปัญหา

1. ตรวจสอบ error message ใน console
2. ตรวจสอบว่ามี serviceAccountKey.json หรือไม่
3. ตรวจสอบ Firestore Rules ว่าอนุญาตให้เขียนข้อมูลได้
4. รัน validate script อีกครั้งเพื่อดูปัญหาที่เหลือ
